anthropic {
  api-key = ""          # override with ANTHROPIC_API_KEY env var
  api-key = ${?ANTHROPIC_API_KEY}
}

db {
  url      = "jdbc:postgresql://localhost:5432/luxvita"
  url      = ${?DATABASE_URL}
  user     = "luxvita"
  user     = ${?DB_USER}
  password = "luxvita"
  password = ${?DB_PASSWORD}
}

server {
  port = 8080
  port = ${?PORT}
}

jwt {
  secret     = "change-me-in-production"
  secret     = ${?JWT_SECRET}
  expiry-days = 30
}
